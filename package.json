{
  "name": "koa-router-multer",
  "version": "1.3.0",
  "description": "Middleware for handling `multipart/form-data` for koa-router",
  "author": "Hirako2000",
  "license": "MIT",
  "repository": "hirako2000/koa-router-multer",
  "keywords": [
    "form",
    "post",
    "multipart",
    "form-data",
    "formdata",
    "koa",
    "middleware"
  ],
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks",
    "test-ci": "isparta cover _mocha --report lcovonly -- --reporter spec --check-leaks test/*.js",
    "test-cov": "isparta cover _mocha -- --reporter dot --check-leaks test/*.js"
  },
  "devDependencies": {
    "concat-stream": "^1",
    "form-data": "^2",
    "isparta": "^4",
    "koa": "^2",
    "koa-router": "^7",
    "mocha": "^3",
    "on-finished": "^2"
  },
  "dependencies": {
    "multer": "^1.3.0"
  },
  "engines": {
    "node": ">= 4"
  },
  "files": [
    "History.md",
    "LICENSE",
    "README.md",
    "index.js"
  ]
}
